---
title: "Untitled"
format: html
---

```{r}
library(anndataR)
library(SingleCellExperiment)
library(scDblFinder)
```

```{r}
adata <- read_h5ad("1_carpallcohort3_cellbender_corrected.h5ad", as = "SingleCellExperiment")
adata
```

```{r}
assay(adata, "counts") <- assay(adata, "X")
adata <- scDblFinder(adata, samples = "Sample_id")
```

```{r}
colData(adata)$doublet_score <- colData(adata)$scDblFinder.score
colData(adata)$doublet_class <- colData(adata)$scDblFinder.class
```

```{r}
table(adata$doublet_class, adata$Sample_id)
adata <- as_AnnData(adata)
adata
```
```{r}
write_h5ad(adata, path ="2_carpallcohort3_doublets_identified.h5ad", mode = "w")
```